apiVersion: apps/v1
kind: Deployment
metadata:
  name: kserve-controller-manager
  namespace: kserve
spec:
  template:
    spec:
      containers:
      # Change the value of image field below to your controller image URL
      - image: image-registry-openshift-image-registry.apps.pierdipi.osp.rh-ods.com/pierdipi/kserve-controller:68731fa37
        name: manager
