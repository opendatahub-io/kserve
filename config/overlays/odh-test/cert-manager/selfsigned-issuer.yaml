# This is the root ClusterIssuer to bootstrap the RHAII CA.
# It can be replaced with a different ClusterIssuer (e.g., Vault)
# for production deployments.
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: rhaii-selfsigned-issuer
spec:
  selfSigned: { }
